# Linux, use libvirt
# MacOS, vbox or fusion

# Debian 10 - Buster
Vagrant.configure("2") do |config|
  config.vm.box = "debian/buster64"
end

# If on MacOS, use vbox or vmware fusion
# Ubuntu 18.04 - Bionic64
Vagrant.configure("2") do |config|
  config.vm.box = "ubuntu/bionic64"
end
